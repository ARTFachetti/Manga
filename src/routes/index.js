const mangas = require('../routes/manga')
const capitulos = require('../routes/capitulos')
const paginas = require('../routes/paginas')



module.exports = {
    mangas,
    capitulos,
    paginas,
}
